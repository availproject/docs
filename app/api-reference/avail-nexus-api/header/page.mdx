import { Tabs, Callout } from 'nextra/components'

# Fetch Header by Avail Block Hash

Use this endpoint to get the Nexus header information for a specific Avail block using it's hash.

### Params

- `hash`: Avail block hash (hex-encoded string)

### Response

- Nexus header information, including:

    **Still in progress**

### Example Request

<Callout type="info">
The `hex-encoded` params must be supplied without the `0x` prefix.
</Callout>

```bash
curl "https://dev.nexus.avail.tools/header?hash=21be1d236b00babc9751852be1b234a0006da45727cc79a872ed01757df36299"
```

<details className="border p-3 rounded-md bg-[#EFF6FF]">
<summary> Sample Response:</summary>

```json
I could not get a response from the API for this method even though the block hash was valid
```
</details>