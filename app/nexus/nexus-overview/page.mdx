---
image: "/img/docs-link-preview.png"
title: Nexus SDK Overview
---

import { Callout, Steps, Tabs } from 'nextra/components';
import { Cards } from 'nextra/components';
import { FileIcon, GithubIcon24, LinkIcon } from '@components/icons'

# Nexus SDK Overview

A powerful TypeScript SDK for cross-chain operations, token bridging, and unified balance management across multiple EVM chains.

<Callout>
**LOOKING FOR PLUG-n-PLAY REACT COMPONENTS?**<br/>
Check out our powerful shadcn-based component library, [Nexus Elements](/nexus/avail-nexus-sdk/nexus-elements), built on top of nexus-core.
</Callout>

## Nexus Core

The core functionality of the Nexus SDK is provided by the `@avail-project/nexus-core` package.

- [**@avail-project/nexus-core**](https://github.com/availproject/nexus-sdk/tree/main/packages/core): Headless SDK for cross-chain operations
    - No React dependencies required
    - Direct Chain Abstraction integration

### Install:

<Tabs items={['pnpm', 'npm', 'yarn']}>

<Tabs.Tab>
```bash filename="Terminal"
pnpm add @avail-project/nexus-core@1.0.0-rc.3
```
</Tabs.Tab>

<Tabs.Tab>
```bash filename="Terminal"
npm install @avail-project/nexus-core@1.0.0-rc.3
```
</Tabs.Tab>

<Tabs.Tab>
```bash filename="Terminal"
yarn add @avail-project/nexus-core@1.0.0-rc.3
```
</Tabs.Tab>

</Tabs>

<Cards num={2}>
    <Cards.Card title="Nexus Core SDK Reference" href="/nexus/avail-nexus-sdk" icon={<FileIcon />} />
    <Cards.Card title="Nexus Core Quickstart" href="/nexus/nexus-quickstart" icon={<FileIcon />} />
</Cards>

## Nexus Elements

A shadcn-based component library built on top of nexus-core that allows devs to integrate working components into their project conveniently.

<Cards num={2}>
    <Cards.Card title="Get Started" href="/nexus/avail-nexus-sdk/nexus-elements" icon={<FileIcon />} />
    <Cards.Card title="Nexus Elements in action" href="https://elements.nexus.availproject.org/" icon={<LinkIcon />} arrow target="_blank" rel="noopener noreferrer" />
    <Cards.Card title="Nexus Elements Quickstart" href="/nexus/nexus-quickstart/nexus-elements" icon={<FileIcon />} />
</Cards>

<Callout>
1. Nexus Core SDK still requires an injected wallet provider (e.g. MetaMask, WalletConnect, etc.)
2. It is designed for client-side browser environments and **cannot be used** in any server-side environments.
</Callout>

## `create-liquid-apps` CLI

A CLI tool that helps you scaffold a new application with Nexus SDK using your preferred framework and auth provider.

<Tabs items={['pnpm', 'npm', 'bun']}>

<Tabs.Tab>
```bash filename="Terminal"
pnpx create-liquid-apps
```
</Tabs.Tab>

<Tabs.Tab>
```bash filename="Terminal"
npx create-liquid-apps
```
</Tabs.Tab>

<Tabs.Tab>
```bash filename="Terminal"
bunx create-liquid-apps
```
</Tabs.Tab>

</Tabs>

You will be prompted to choose your preferred framework and auth provider in a UI that looks like this:
<br/>
<p align="center">
<img src="/img/nexus-overview/create-liquid-apps-1.png" alt="create-liquid-apps" width="100%" />
</p>

Alternatively, you can skip the UI and use the CLI with options:
```bash filename="Terminal"
pnpx create-liquid-apps my-app --framework next --widgets nexus-core --auth wagmi-familyconnect
```

Check out the package's NPM page for more details:

<Cards num={2}>
    {/* <Cards.Card title="CLI Github Repo" href="https://github.com/availproject/create-liquid-app" icon={<GithubIcon24 />} arrow target="_blank" rel="noopener noreferrer" /> */}
    <Cards.Card title="CLI NPM Package" href="https://www.npmjs.com/package/create-liquid-apps" icon={<LinkIcon />} arrow target="_blank" rel="noopener noreferrer" />
</Cards>