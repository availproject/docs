---
title: Components
description: Browse all available Nexus UI Element components.
---

A shadcn-based component library built on top of nexus-core that allows devs to integrate working components into their project conveniently.
Nexus Elements currently provides the following components:

1. Unified Balance
2. View History
3. Fast Bridge
4. Fast Transfer
5. Deposit
6. Swaps

Refer to the Nexus Elements website for detailed usage docs:

<Cards num={2}>
    <Card title="Avail Nexus Elements" href="https://elements.nexus.availproject.org/" icon={<LinkIcon />}  arrow target="_blank" rel="noopener noreferrer" />
    <Card title="Nexus Elements Github Repo" href="https://github.com/availproject/nexus-elements" icon={<GithubIcon24 />}  arrow target="_blank" rel="noopener noreferrer" />
</Cards>

## Install

1. Install `shadcn/ui` into your project. Refer to [shadcn/ui installation guide](https://ui.shadcn.com/docs/installation) for more details.

2. Install the components you need using the shadcn CLI.

<Tabs defaultValue="unified-balance">
  <TabsList>
    <TabsTrigger value="unified-balance">Unified Balance</TabsTrigger>
    <TabsTrigger value="view-history">View History</TabsTrigger>
    <TabsTrigger value="fast-bridge">Fast Bridge</TabsTrigger>
    <TabsTrigger value="fast-transfer">Fast Transfer</TabsTrigger>
    <TabsTrigger value="deposit">Deposit</TabsTrigger>
    <TabsTrigger value="swaps">Swaps</TabsTrigger>
  </TabsList>
  <TabsContent value="unified-balance">

```bash title="pnpm"
pnpm dlx shadcn@latest add @nexus-elements/unified-balance
```

```bash title="npm"
npx shadcn@latest add @nexus-elements/unified-balance
```

```bash title="yarn"
yarn shadcn@latest add @nexus-elements/unified-balance
```

  </TabsContent>
  <TabsContent value="view-history">

```bash title="pnpm"
pnpm dlx shadcn@latest add @nexus-elements/view-history
```

```bash title="npm"
npx shadcn@latest add @nexus-elements/view-history
```

```bash title="yarn"
yarn shadcn@latest add @nexus-elements/view-history
```

  </TabsContent>
  <TabsContent value="fast-bridge">

```bash title="pnpm"
pnpm dlx shadcn@latest add @nexus-elements/fast-bridge
```

```bash title="npm"
npx shadcn@latest add @nexus-elements/fast-bridge
```

```bash title="yarn"
yarn shadcn@latest add @nexus-elements/fast-bridge
```

  </TabsContent>
  <TabsContent value="fast-transfer">

```bash title="pnpm"
pnpm dlx shadcn@latest add @nexus-elements/fast-transfer
```

```bash title="npm"
npx shadcn@latest add @nexus-elements/fast-transfer
```

```bash title="yarn"
yarn shadcn@latest add @nexus-elements/fast-transfer
```

  </TabsContent>
  <TabsContent value="deposit">

```bash title="pnpm"
pnpm dlx shadcn@latest add @nexus-elements/deposit
```

```bash title="npm"
npx shadcn@latest add @nexus-elements/deposit
```

```bash title="yarn"
yarn shadcn@latest add @nexus-elements/deposit
```

  </TabsContent>
  <TabsContent value="swaps">

```bash title="pnpm"
pnpm dlx shadcn@latest add @nexus-elements/swaps
```

```bash title="npm"
npx shadcn@latest add @nexus-elements/swaps
```

```bash title="yarn"
yarn shadcn@latest add @nexus-elements/swaps
```

  </TabsContent>
</Tabs>

### Next Steps

<Cards num={2}>
    <Card title="Nexus Elements Installation" href="/docs/nexus/nexus-ui-elements/installation" icon={<FileIcon />} />
    <Card title="Nexus Elements template repo" href="https://github.com/availproject/nexus-elements-template" icon={<GithubIcon24 />}  arrow target="_blank" rel="noopener noreferrer" />
</Cards>
