import { Tabs } from 'nextra/components'
import { Callout } from 'nextra/components'

# Get API versions supported on the server

### Request

- `GET /versions`


<Tabs items={['CURL']}>

<Tabs.Tab>
    
```sh
curl http://localhost:8080/versions
```

</Tabs.Tab>
</Tabs>

### Response

```json filename="Example JSON response"
["v1"]
```