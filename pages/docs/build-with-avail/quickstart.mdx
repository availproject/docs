---
id: quickstart
title: Get Started with Avail
sidebar_label: Quickstart
description: 'Discover how to utilize Avail as a data availability layer.'
keywords:
  - documentation
  - avail
  - develop
  - build
  - data availability
  - da
image: https://docs.availproject.org/img/avail/AvailDocs.png
---
import { Tabs } from 'nextra/components'


# Get Started with Avail

## Start Operating

{
/*
-- To run an Avail light client, simply install the [Avail CLI `npm` package](https://github.com/availproject/cli):

```bash
npm i -g @availproject/cli
```

Then, run:

```bash
avail lc up
```
*/
}

To run an Avail light client, simply run this command powered by [availup](https://github.com/availproject/availup):

```bash
curl -sL1 avail.sh | bash
```



And that's it!

If you're seeking more control over the configuration or are interested in running a full node, check out the deployment guides below.

| Action                                                                   | Tokens Needed | Staking | Technical Skill Required | Client Binary                                                                        |
| ------------------------------------------------------------------------ | ------------- | ------- | ------------------------ | ------------------------------------------------------------------------------------ |
| [<ins>Run a Light Client</ins>](/docs/operate-a-node/run-a-light-client/0010-light-client) | No            | No      | Basic                    | [<ins>v1.11.1</ins>](https://github.com/availproject/avail-light/releases/tag/avail-light-client-v1.11.1) |
| [<ins>Run a Full Node / Validator</ins>](/docs/operate-a-node/run-a-full-node/full-node)           | No            | No      | Moderate                 | [<ins>v2.2.4.1</ins>](https://github.com/availproject/avail/releases/tag/v2.2.4.1) |

---

## Start Building

Avail API is a set of tools and resources that allows developers to interact with Avail Nodes and with the Avail DA Network.
The API is build around existing tools (`polkadotjs`, `subxt`, `gsrpc`) with additional types and quality of life interfaces added
on top of them.

The API is available in three languages: `Typescript`, `Rust`, and `Go`.

### Avail API Setup
<Tabs items={['Typescript', 'Rust', 'Go']}>

<Tabs.Tab>
Create an empty directory and add the following files there:
- `index.ts` with the following content:
```ts
import { SDK } from "avail-js-sdk"

const main = async () => {
  // Main Content

  process.exit()
}
main()
```
- `package.js` with the following content:
```json
{
  "name": "example",
  "version": "0.0.1",
  "description": "example",
  "dependencies": {
    "avail-js-sdk": "^0.3.0"
  },
  "resolutions": {
    "ws": ">=8.17.1"
  }
}
```

Your directory should now have the following files: `index.ts`, and `package.json`.

The example can be run with the runtime of your choice. We tested it wih the following runtimes:
- Node (TS-Node): Node v20.0.0; TS-Node v10.9.2; NPM v9.6.4
- Deno: v2.0.2
- Bun: v1.1.33

If you are using an newer version it should work just fine as well.
<Tabs items={['TS-Node', 'Deno', 'Bun']}>

<Tabs.Tab>
1. Install [Node](https://nodejs.org/en/download/package-manager)
2. Install [TS-Node](https://www.npmjs.com/package/ts-node)
3. Add a file named `tsconfig.json` to your current directory with the following content:
```ts
{
  "compilerOptions": {
    "target": "es2016",
    "module": "commonjs",
    "outDir": "build",
    "declaration": true,
    "esModuleInterop": true,
    "forceConsistentCasingInFileNames": true,
    "strict": true,
    "skipLibCheck": true,
    "noUnusedParameters": true
  },
  "include": ["src"],
  "exclude": ["node_modules", "build"]
}
```
4. Run example with `ts-node ./index.ts`

Your directory should now have the following files: `index.ts`, `package.json`, and `tsconfig.json`.
</Tabs.Tab>

<Tabs.Tab>
1. Install [Deno](https://docs.deno.com/runtime/getting_started/installation)
2. Run example with `deno run -A ./index.ts`
</Tabs.Tab>

<Tabs.Tab>
1. Install [Bun](https://bun.sh/docs/installation)
2. Run example with `bun run ./index.ts`
</Tabs.Tab>
</Tabs>
</Tabs.Tab>

<Tabs.Tab>
1. Install [Rust](https://www.rust-lang.org/tools/install) and then create an empty directory and add the following files there:
- `main.rs` with the following content:
```rust
use avail_rust::SDK;

#[tokio::main]
async fn main() -> Result<(), String> {
    // Main Content

    Ok(())
}
```
- `Cargo.toml` with the following content:
```toml
[package]
name = "example"
edition = "2021"
version = "0.1.0"

[[bin]]
name = "example"
path = "./main.rs"

[dependencies]
avail-rust = { git = "https://github.com/availproject/avail", tag = "v2.2.5.1" }
tokio = { version = "1.38.0", features = ["rt-multi-thread"] }
```
Your directory should now have the following files: `main.rs`, and `Cargo.toml`.

2. Run example with `cargo run` 

</Tabs.Tab>

<Tabs.Tab>
TODO
</Tabs.Tab>
</Tabs>

With all the setup being done, we are now ready to explore what the API can offer.
Keep in mind that the next examples are going to contain only the snippets that we need to add to our code.
If you get lost, at the end of this page you can find the full example with everything that will be added in
the next sections.

### Create an API Instance
Creating an instance of the SDK is quite straightforward. The only argument that needs to be passed is the
provider endpoint that we connect to. For this example, we are establishing a connection to a locally run
Node. For testing purposes this is exactly what we want.

Make sure that you are already running a node locally otherwise it's not going to be possible to run the example.

<Tabs items={['Typescript', 'Rust', 'Go']}>

<Tabs.Tab>
```ts
// Start of main function
// Main Content
const sdk = await SDK.New("ws://127.0.0.1:9944")
console.log("Genesis Hash: " + sdk.api.genesisHash.toHex())
```

</Tabs.Tab>

<Tabs.Tab>
```rust
// Start of main function
// Main Content
let sdk = SDK::new("ws://127.0.0.1:9944").await.unwrap();
println!("Genesis Hash: {:?}", sdk.api.genesis_hash());
```

</Tabs.Tab>

<Tabs.Tab>
```go
TODO
```
</Tabs.Tab>
</Tabs>

Now run the example and if you see an output similar to this:
```
Genesis Hash: 0x3c29e7a3fc90796e33b5d2b26506d5cd9177ec7be2e38ed641e1538c8938026c
```
It means that the connection was successful and we got back the genesis hash of the locally running network.

### Keyring
Keyrings are our window to account management. Without an account, we cannot do transactions so creating one is paramount.

<Tabs items={['Typescript', 'Rust', 'Go']}>

<Tabs.Tab>
```ts
// Replace the import with the following line
import { SDK, Keyring } from "avail-js-sdk";

// Start of main function
// Main Content
// ...

// Account Creation
const account = new Keyring({ type: "sr25519" }).addFromUri("//Alice")
console.log("The address of Alice is: " + account.address)
```
</Tabs.Tab>

<Tabs.Tab>
```rust
// Replace the import with the following lines
use avail_rust::{SDK, Keypair, SecretUri};
use core::str::FromStr;

// Start of main function
// Main Content
// ...

// Account Creation
let secret_uri = SecretUri::from_str("//Alice").unwrap();
let account = Keypair::from_uri(&secret_uri).unwrap();
println!("The address of Alice is: {}", account.public_key().to_account_id().to_string());
```
</Tabs.Tab>

<Tabs.Tab>
```go
TODO
```
</Tabs.Tab>
</Tabs>

You should see the following output:
```
The address of Alice is: 5GrwvaEF5zXb26Fz9rcQpDWS57CtERHpNehXCPcNoHGKutQY
```

### Create Application Key
Data submission is the bread and butter of Avail DA Network. When submitting data we are prompt to enter our application key
that will be used with that data.

<Tabs items={['Typescript', 'Rust', 'Go']}>

<Tabs.Tab>
```ts
// Replace the import with the following line
import { SDK, Keyring, WaitFor } from "avail-js-sdk";

// Account Creation
// ...

// Application Key Creation
const appKeyResult = await sdk.tx.dataAvailability.createApplicationKey("My Personal Application Key", WaitFor.BlockInclusion, account)
if (appKeyResult.isErr) {
  console.log("Error: " + appKeyResult.reason)
  process.exit(1)
}

const appKeyId = parseInt(appKeyResult.event.id)
console.log("Application key ID: " + appKeyId)
```
</Tabs.Tab>

<Tabs.Tab>
```rust
// Replace the import with the following line
use avail_rust::{Key, Keypair, SecretUri, WaitFor, SDK, Nonce, Options};

// Account Creation
// ...

// Application Key Creation
let key = String::from("My Personal Application Key").as_bytes().to_vec();
let key = Key { 0: key };
let options = Options::new().nonce(Nonce::BestBlockAndTxPool);

let result = sdk.tx.data_availability.create_application_key(key, WaitFor::BlockInclusion, &account, Some(options)).await?;
println!("Application key ID: {}", result.event.id.0);
```
</Tabs.Tab>

<Tabs.Tab>
```go
TODO
```
</Tabs.Tab>
</Tabs>

You should see the following output:
```
Application key ID: 10
```

Note: From now on if you execute the same example more than once you will be greeted with the following error message
- Typescript - `Error: dataAvailability.AppKeyAlreadyExists: The application key already exists.`
- Rust - `Error: "Runtime error: Pallet error: DataAvailability::AppKeyAlreadyExists"`
- Go - TODO

Don't panic, this is expected because application keys are supposed to be unique so there cannot be two application key with the same name.
Simply changing the key name from `My Personal Application Key` to `My Personal Application Key 2` will solve the issue

### Data Submission
<Tabs items={['Typescript', 'Rust', 'Go']}>

<Tabs.Tab>
```ts
// Replace the import with the following line
import { SDK, Keyring, WaitFor, TransactionOptions } from "avail-js-sdk";

// Application Key Creation
// ...

// Data Submission
const options: TransactionOptions = { app_id: appKeyId };
const daResult = await sdk.tx.dataAvailability.submitData("My Data", WaitFor.BlockInclusion, account, options)
if (daResult.isErr) {
  console.log("Error: " + daResult.reason)
  process.exit(1)
}
console.log("Data Submission Hash: " + daResult.event.dataHash)
```
</Tabs.Tab>

<Tabs.Tab>
```rust
// Replace the import with the following line
use avail_rust::{Key, Keypair, SecretUri, WaitFor, SDK, Nonce, Options, Data};

// Application Key Creation
// ...

// Data Submission
let data = String::from("My Data").as_bytes().to_vec();
let data = Data { 0: data };
let options = Options::new().nonce(Nonce::BestBlockAndTxPool).app_id(result.event.id.0);

let result = sdk.tx.data_availability.submit_data(data, WaitFor::BlockInclusion, &account, Some(options)).await?;
println!("Data Submission Hash: {:?}", result.event.data_hash);
```
</Tabs.Tab>

<Tabs.Tab>
```go
TODO
```
</Tabs.Tab>
</Tabs>

You should see the following output:
```
Data Submission Hash: 0x7d5ece96efb6832141afe1af96b337d5fec3839bd3cedd31317e4fa7279001e0
```

### Balance Transfer
<Tabs items={['Typescript', 'Rust', 'Go']}>

<Tabs.Tab>
```ts
// Data Submission
// ...

// Balance Transfer
const bobAddress = "5FHneW46xGXgs5mUiveU4sbTyGBzmstUspZC92UhjJM694ty"
const oneAvail = new BN(10).pow(new BN(18))
const btResult = await sdk.tx.balances.transferKeepAlive(bobAddress, oneAvail, WaitFor.BlockInclusion, account)
if (btResult.isErr) {
  console.log("Error: " + btResult.reason)
  process.exit(1)
}

const amount = btResult.event.amount;
console.log(`Transfer completed. Alice transferred to Bob ${amount} units`)
```
</Tabs.Tab>

<Tabs.Tab>
```rust
// Data Submission
// ...

// Balance Transfer
let bob_address = "5FHneW46xGXgs5mUiveU4sbTyGBzmstUspZC92UhjJM694ty";
let one_avail = 1_000_000_000_000_000_000u128;
let options = Options::new().nonce(Nonce::BestBlockAndTxPool);

let result = sdk.tx.balances.transfer_keep_alive(bob_address, one_avail, WaitFor::BlockInclusion, &account, Some(options)).await?;
println!("Transfer completed. Alice transferred to Bob {:?} units", result.event.amount);
```
</Tabs.Tab>

<Tabs.Tab>
```go
TODO
```
</Tabs.Tab>
</Tabs>

You should see the following output:
```
Transfer completed. Alice transferred to Bob 1000000000000000000 units
```

### Full Example
<Tabs items={['Typescript', 'Rust', 'Go']}>

<Tabs.Tab>
```ts
import { SDK, Keyring, WaitFor, TransactionOptions, BN } from "avail-js-sdk";

const main = async () => {
  // Start of main function
  // Main Content
  const sdk = await SDK.New("ws://127.0.0.1:9944")
  console.log("Genesis Hash: " + sdk.api.genesisHash.toHex())

  // Account Creation
  const account = new Keyring({ type: "sr25519" }).addFromUri("//Alice")
  console.log("The address of Alice is: " + account.address)

  // Application Key Creation
  const appKeyResult = await sdk.tx.dataAvailability.createApplicationKey("My Personal Application Key", WaitFor.BlockInclusion, account)
  if (appKeyResult.isErr) {
    console.log("Error: " + appKeyResult.reason)
    process.exit(1)
  }

  const appKeyId = parseInt(appKeyResult.event.id)
  console.log("Application key ID: " + appKeyId)

  // Data Submission
  const options: TransactionOptions = { app_id: appKeyId };
  const daResult = await sdk.tx.dataAvailability.submitData("My Data", WaitFor.BlockInclusion, account, options)
  if (daResult.isErr) {
    console.log("Error: " + daResult.reason)
    process.exit(1)
  }
  console.log("Data Submission Hash: " + daResult.event.dataHash)

  // Balance Transfer
  const bobAddress = "5FHneW46xGXgs5mUiveU4sbTyGBzmstUspZC92UhjJM694ty"
  const oneAvail = new BN(10).pow(new BN(18))
  const btResult = await sdk.tx.balances.transferKeepAlive(bobAddress, oneAvail, WaitFor.BlockInclusion, account)
  if (btResult.isErr) {
    console.log("Error: " + btResult.reason)
    process.exit(1)
  }

  const amount = btResult.event.amount;
  console.log(`Transfer completed. Alice transferred to Bob ${amount} units`)

  process.exit()
};

main();
```
</Tabs.Tab>

<Tabs.Tab>
```rust
use avail_rust::{Key, Keypair, SecretUri, WaitFor, SDK, Nonce, Options, Data};
use core::str::FromStr;

#[tokio::main]
async fn main() -> Result<(), String> {
    // Start of main function
    // Main Content
    let sdk = SDK::new("ws://127.0.0.1:9944").await.unwrap();
    println!("Genesis Hash: {:?}", sdk.api.genesis_hash());

    // Account Creation
    let secret_uri = SecretUri::from_str("//Alice").unwrap();
    let account = Keypair::from_uri(&secret_uri).unwrap();
    println!("The address of Alice is: {}", account.public_key().to_account_id().to_string());

    // Application Key Creation
    let key = String::from("My Personal Application Key").as_bytes().to_vec();
    let key = Key { 0: key };
    let options = Options::new().nonce(Nonce::BestBlockAndTxPool);

    let result = sdk.tx.data_availability.create_application_key(key, WaitFor::BlockInclusion, &account, Some(options)).await?;
    println!("Application key ID: {}", result.event.id.0);

    // Data Submission
    let data = String::from("My Data").as_bytes().to_vec();
    let data = Data { 0: data };
    let options = Options::new().nonce(Nonce::BestBlockAndTxPool).app_id(result.event.id.0);

    let result = sdk.tx.data_availability.submit_data(data, WaitFor::BlockInclusion, &account, Some(options)).await?;
    println!("Data Submission Hash: {:?}", result.event.data_hash);

    // Balance Transfer
    let bob_address = "5FHneW46xGXgs5mUiveU4sbTyGBzmstUspZC92UhjJM694ty";
    let one_avail = 1_000_000_000_000_000_000u128;
    let options = Options::new().nonce(Nonce::BestBlockAndTxPool);

    let result = sdk.tx.balances.transfer_keep_alive(bob_address, one_avail, WaitFor::BlockInclusion, &account, Some(options)).await?;
    println!("Transfer completed. Alice transferred to Bob {:?} units", result.event.amount);

	Ok(())
}
```
</Tabs.Tab>

<Tabs.Tab>
```go
TODO
```
</Tabs.Tab>
</Tabs>

You should see the following output:
```
Genesis Hash: 0x3c29e7a3fc90796e33b5d2b26506d5cd9177ec7be2e38ed641e1538c8938026c
The address of Alice is: 5GrwvaEF5zXb26Fz9rcQpDWS57CtERHpNehXCPcNoHGKutQY
Application key ID: 10
Data Submission Hash: 0x7d5ece96efb6832141afe1af96b337d5fec3839bd3cedd31317e4fa7279001e0
Transfer completed. Alice transferred to Bob 1000000000000000000 units
```
