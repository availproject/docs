import SwapWidget from "../swaps/swap-widget";
import ShowcaseWrapper from "./showcase-wrapper";

const SwapsShowcase = () => {
  return (
    <ShowcaseWrapper type="swaps">
      <SwapWidget />
    </ShowcaseWrapper>
  );
};

export default SwapsShowcase;
